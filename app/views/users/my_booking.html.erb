<table class="table table-hover"  width="10%">
  <tr>
     <th colspan="2">Booking</th>
  </tr>
  <tr>
    <th>Date</th>
    <th>Cleaner name</th>
  </tr>
  <% @bookings.each do |book| %>
    <tr>
	    <td><%= book.date %></td>
      <td><%= user_fname(book.cleaner_id) %> <%= user_lname(book.cleaner_id) %></td>
   </tr>
  <% end %>
</table>
